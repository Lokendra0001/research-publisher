import { PAGE_TITLES } from "@/utils/constant";

export const metadata = {
    title: PAGE_TITLES.GUIDELINES,
};

export default function GuidelinesLayout({ children }) {
    return <>{children}</>;
}
